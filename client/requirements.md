## Packages
react-leaflet | Map component library for React
leaflet | Core mapping library
framer-motion | Smooth animations for sidebar and interactions
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit', sans-serif"],
  body: ["'DM Sans', sans-serif"],
}
Leaflet CSS must be imported in index.css or main.tsx
Images: Static assets for markers if needed, or use Lucide icons inside custom DivIcons
